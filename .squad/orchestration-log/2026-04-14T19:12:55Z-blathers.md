# Orchestration Log: Blathers — 2026-04-14T19:12:55Z

## Spawn Context

- **Topic:** Investigate failed localhost-auth-playwright GitHub Actions run 24415783660
- **Requested by:** Jonny Muir
- **Mode:** background

## Outcome

**Status:** ✅ Complete

Traced workflow and bootstrap setup against the Aspire auth lane. Confirmed the smallest next fix needed in ci-tests.yml for Linux certificate trust before rerunning the lane.

**Key finding:** The job stopped at **Trust .NET development certificate** with exit code 4 and never reached `Validate Aspire prerequisites` or the Playwright test execution. Runner explicitly reported `SSL_CERT_DIR` misconfiguration.

**Analysis confirmed:** Workflow path filters already include the full Aspire auth graph; job structure is sound. Only the certificate bootstrap for Linux runners needs `SSL_CERT_DIR` wiring.

## Decision Produced

**File:** `.squad/decisions/inbox/blathers-auth-failure-investigation.md`

### Summary

Treat GitHub Actions run 24415783660 as a workflow bootstrap failure. The next change should be a minimal workflow fix: on Ubuntu, export/persist `SSL_CERT_DIR` so it includes `$HOME/.aspnet/dev-certs/trust` and the system certificate directories before running `dotnet dev-certs https --trust`.

### Smallest Correct Fix Next

1. Keep the existing job structure, path filters, browser install, and working directory.
2. Update only the certificate bootstrap for Linux runners so `SSL_CERT_DIR` is set/persisted before `dotnet dev-certs https --trust`.
3. Rerun the lane and only then investigate Docker/Aspire/runtime issues if the job proceeds past certificate setup.

---

**Scribed by:** Scribe at 2026-04-14T19:12:55Z
