# Tom Nook — Orchestration Log 2026-03-28T12:19:26Z

## Spawn Mode: Background

## Task: Biometric Auth Design — JWT/UUID Consistency Fix

**Status:** Completed

**Trigger:** JWT/UUID inconsistency detected in `Design/biometric-auth.md` — earlier sections described BiometricToken as a UUID v4, while the agreed architecture adopted a signed JWT. Inconsistency introduced confusion around token format, lifetime, rate limiting, and audit logging scope.

### Changes Made

**`/Design/biometric-auth.md`:**
- Replaced all references to `BiometricToken` as UUID v4 with signed JWT
- Added `DeviceId` claim to JWT payload (client-generated UUID, stored on first launch)
- Updated DB schema: `prismBiometricTokens` stores `DeviceId` alongside token hash
- `/exchange` endpoint now validates `deviceId` claim against DB row (closes bearer theft vector)
- Token lifetime changed from 90 days (non-configurable) to 30 days default, tenant-configurable (range: 7–90 days)
- Audit logging (attempt + outcome + token ID + IP) promoted from v2 to v1 requirement
- Rate limiting updated: 3 failed attempts / 10 min per token → token locked (requires re-registration); IP-based as secondary layer; removed unenforceable "5 requests/minute per device ID" policy

### Decision Filed

- `.squad/decisions/inbox/tom-nook-biometric-jwt-committed.md` — captures all above as accepted decisions for merge into ledger
