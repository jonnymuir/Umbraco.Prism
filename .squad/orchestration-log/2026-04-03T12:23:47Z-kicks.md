# Orchestration Log: Kicks — Phase 3 Capacitor Push Notifications

**Date:** 2026-04-03T12:23:47Z  
**Agent:** Kicks (Mobile Native Specialist)  
**Phase:** 3 of Push Notifications feature  
**Mode:** Background spawn  

---

## Summary

Kicks successfully implemented Phase 3 of the push notifications feature, integrating the Capacitor push notifications plugin into the TypeScript client codebase and exposing push notification configuration via the bundle generator.

---

## Deliverables

### 1. Push Notifications API (`PrismPushNotifications`)

**File:** `src/UmbracoPrism.Client/src/backoffice/push-notifications.ts`

**Public Methods (8):**
1. `isSupported(): Promise<boolean>` — Check platform support
2. `registerDevice(callback: (token) => Promise<void>): Promise<string>` — Request permissions and register device token
3. `unregisterDevice(): Promise<void>` — Unregister device token
4. `subscribeToGenre(genreId: string): Promise<void>` — Subscribe to genre topic
5. `unsubscribeFromGenre(genreId: string): Promise<void>` — Unsubscribe from genre topic
6. `getPermissionStatus(): Promise<PushPermissionState>` — Get current permission status
7. `requestPermission(): Promise<void>` — Explicitly request permissions (for permission request UI)
8. `handleNotificationFromUrl(url: string): void` — Handle incoming notifications (useful on app resume)

**Design Decisions:**
- Graceful web degradation (returns no-op on web platform, fully functional on native)
- Permission-first flow (does not assume permissions granted)
- Error logging (catches and logs Capacitor plugin errors)
- Stateless API (all operations are idempotent)

### 2. Bundle Generator Integration

**File:** `src/UmbracoPrism.Client/src/backoffice/prism-create-tenant-modal.ts`

**Changes:**
- Added `pushNotificationsEnabled: boolean` toggle to tenant modal (default: `false`)
- Toggle controls whether push notification dependencies are included in the generated mobile bundle
- When enabled, bundle includes `@capacitor/push-notifications` and `PrismPushNotifications` API

### 3. Package Dependency

**File:** `src/UmbracoPrism.Client/package.json`

**Added:**
- `@capacitor/push-notifications@^7.0.0` — Official Ionic-maintained Capacitor plugin

**Rationale:** Lighter footprint than `@capacitor-firebase/messaging`, aligns with Capacitor 7.x ecosystem, APNs-native on iOS.

### 4. Documentation

**File:** `docs/PUSH_SETUP.md`

**Contents:**
- iOS setup: APNs p8 key generation, Xcode capability configuration, entitlements
- Android setup: Firebase Console project, `google-services.json` download, Gradle config
- Troubleshooting: Common issues and debugging steps
- Permission request best practices and recommended timing (post-biometric-login)

### 5. Public API Export

**File:** `src/UmbracoPrism.Client/src/backoffice/index.ts`

**Added:**
- `export { PrismPushNotifications }` — Main API class
- `export { PushPermissionState }` — Permission status enum (granted, denied, prompt)

---

## Build Status

✅ TypeScript build: 0 errors  
✅ `npm run build` passes cleanly  
✅ All type definitions compile  

---

## Testing

- Type checking: TypeScript strict mode ✅
- Permission request flow tested conceptually (cannot test actual platform permissions in build environment)
- Capacitor API contracts validated against v7.0.0 types
- Web platform graceful degradation verified

---

## Architecture Notes

- **Plugin:** `@capacitor/push-notifications` handles platform-specific registration
- **Backend Contract:** API endpoints defined in `docs/design/notifications-backend.md` (not yet implemented by Blathers)
- **Permission Timing:** Left to consumers (recommended post-biometric-login, not hard-coded)
- **Native Setup:** iOS/Android config is manual consumer task (documented in `PUSH_SETUP.md`)

---

## Team Dependencies

**Blathers (Backend — Phase 4 blocker):**
- Must implement POST `/umbraco/prism/push/register`
- Must implement DELETE `/umbraco/prism/push/register`
- Must implement POST `/umbraco/prism/push/subscribe`
- Must implement DELETE `/umbraco/prism/push/unsubscribe`

**Tom Nook (Services):** May need to update `MobileBundleService.cs` to conditionally scaffold push notification setup code when `pushNotificationsEnabled: true`.

---

## Future Enhancements

1. Auto-inject `registerDevice()` call into biometric login success flow when push is enabled
2. Generate Android notification channel setup code in bundle
3. Interactive CLI setup wizard (`npx prism-setup-push`)
4. Test Push button in tenant modal
5. Optional Firebase Messaging toggle for consumers

---

## Notes

- Phase 3 is **not end-to-end functional** until Blathers implements backend endpoints
- Current implementation is **production-ready from TypeScript perspective** (API contract sound, permission flow correct, error handling robust)
- Flagged as "awaiting backend" in Kicks' history

