# Orchestration: Blathers CI Test Fix

**Timestamp:** 2026-04-14T17:52:43Z  
**Agent:** Blathers  
**Topic:** Fix CI-failing Phase1 security regression tests

## Outcome

Fixed Phase1SecurityRegressionTests by switching loopback OIDC harness from https://localhost to http://127.0.0.1 and aligning discovery HTTPS requirements with the metadata URL scheme.

## Key Changes

- Loopback OIDC authority now runs on `http://127.0.0.1` (not `https://localhost`)
- `PrismOidcConfiguration` uses `HttpDocumentRetriever` with `RequireHttps` derived from metadata address scheme
- Behavior aligns with existing `PrismSigningKeyCache` pattern
- Narrow failing slice and workflow-equivalent core test suite reported green

## Decision Created

- `.squad/decisions/inbox/blathers-ci-test-fix.md` — CI-safe loopback OIDC regression harness

## Status

✅ Complete
