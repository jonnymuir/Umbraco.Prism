# Orchestration Log: tangy-1

**Date:** 2026-05-16T18:15:00Z  
**Agent:** tangy (Testing seam: authoring fixture, agent-loop, graph keyboard)  
**Task:** V1 foundation — test seam scaffolding (skipped tests + fixture lock)  
**Outcome:** ✅ Complete

## Summary

Tangy established the V1 test seam: C# fixture tests, Playwright agent-loop and keyboard specs. Planning workflow fixture locked as source of truth. All suites green: dotnet test 690 passed + 4 skipped; playwright 4 passed + 10 skipped. Commit `916045e`.

## Files Touched

- `.e2e/tests/workflow-editor/planning-workflow-agent-loop.spec.ts` — Playwright agent-loop suite (2 skipped: proposal-request UX pending Isabelle; 2 fixme: full projection pipeline pending wave 2)
- `.e2e/tests/workflow-editor/workflow-graph-keyboard.spec.ts` — Playwright keyboard spec (2 skipped pending Isabelle's dual-mode Storybook; 3 passed covering WCAG 2.1.1 Keyboard + 4.1.2 Name/Role/Value)
- `src/UmbracoPrism.Core.Tests/Workflow/Authoring/PlanningWorkflowFixtureTests.cs` — C# fixture contract tests (4 skipped awaiting `planning.workflow.json` from Blathers; semantic assertions on stages, transitions, roles, fields)

## Test Coverage

**PlanningWorkflowFixtureTests.cs (C# — Authoring/):**
- Four skipped tests awaiting Blathers' `planning.workflow.json` fixture
- Documents expected fixture shape: stages, transitions, roles, fields
- Asserts required stage keys: `application-form`, `check-answers`, `submitted`
- Byte-identical serialize→deserialize round-trip with deterministic JSON options
- Skip message: "Awaiting fixture from Blathers' V1 foundation slice"

**planning-workflow-agent-loop.spec.ts (Playwright):**
- "Author can request a workflow change in natural language and see a proposal diff before applying" (skipped pending Isabelle's conversation-pane Storybook story)
- "Author cannot apply a proposal whose validation status is fail" (skipped pending Isabelle's failing-proposal Storybook story)
- "Applicant cannot submit planning application without identity verification when ID&V step is enabled" (marked test.fixme() — depends on full projection pipeline, wave 2)
- Semantic selectors throughout: data-testid=*, data-proposal-id, getByRole

**workflow-graph-keyboard.spec.ts (Playwright):**
- "Stage list mode supports arrow-key navigation" (skipped pending Isabelle's linear-mode Storybook story)
- "Mode toggle is keyboard accessible and aria-pressed reflects state" (skipped pending Isabelle's dual-mode graph Storybook story)
- Three passed tests asserting WCAG 2.1.1 Keyboard + 4.1.2 Name/Role/Value contracts
- Uses Isabelle's documented hooks: data-testid="toolbar-list-view", data-testid="linear-list"

## Test Seam Contracts

- **Fixture shape:** Locked by Blathers' `planning.workflow.json` (4 stages: `declaration`, `application-form`, `check-answers`, `submitted`)
- **Test data:** Fixture copied to output directory via `<None CopyToOutputDirectory="PreserveNewest" />` in Core.Tests.csproj
- **Accessibility:** All skipped Playwright tests reference WCAG semantics (role=application, aria-pressed, aria-live=polite, keyboard navigation)
- **Semantic coupling:** Tests use data-testid, data-proposal-id, getByRole — no brittle CSS selectors

## Context

Fixture and test infrastructure now in place. Four C# tests waiting for fixture (already satisfied by Blathers); four Playwright tests waiting for component Storybook stories (Isabelle in place); two fixme tests marked for wave 2 (full projection pipeline, agent loop hosting).

## Next

Wave 2: unskip Playwright tests as Isabelle's components move to live routing; implement agent loop MCP server and proposal contract; host backoffice extension. Wave 3: collaborative real-time editing, multi-tenant authoring, permission expressiveness.
