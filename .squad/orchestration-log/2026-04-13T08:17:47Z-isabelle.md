---
timestamp: 2026-04-13T08:17:47Z
agent: Isabelle (Frontend Dev)
session: generic-oidc-secret-refactor
status: completed
---

# Orchestration Log — Isabelle

## Role
Frontend developer; UI alignment and UX for secret reference handling.

## Activities

1. **Aligned tenant modal to replace-only secret reference handling**
   - Updated tenant secret input UI to accept only secret references (e.g., Key Vault key names), not raw secret values
   - Removed direct secret input field for generic OIDC; replaced with provider/reference metadata display
   - Added visual distinction between demo marker (inline, repo-owned) and production references (vault-backed)

2. **Updated Storybook stories**
   - Added demo story showing localhost Keycloak tenant with inline marker
   - Added production story showing vault-backed reference (e.g., `azure-key-vault:Prism-Keycloak-Secret`)
   - Stories document the UI contract and expected behavior

3. **Implemented localhost inline exception UI**
   - Tenant modal detects demo marker (`inline` provider) at runtime
   - For demo tenants: displays "Using repository-owned demo secret" callout
   - For production tenants: displays "Secret managed in Azure Key Vault" callout
   - UX intent clear and accessible to admins

## Outcomes

- ✅ Tenant modal aligned to replace-only reference handling
- ✅ Secret input restricted to references (no raw value upload)
- ✅ Demo vs. production paths visually distinguished in UI
- ✅ Storybook stories document behavior and intent
- ✅ Client build passing

## Blockers Addressed

None — UI alignment straightforward given backend contract lock.

## Notes for Next Scribe Session

Isabelle's UI changes reinforce the secure-by-default posture by making the secret model explicit and preventing accidental raw-value input. The distinction between demo (inline) and production (vault-backed) is now clear in the admin interface.
