# Orchestration Log: Blathers — 2026-04-14T18:06:05Z

**Topic:** Add separate GitHub Actions job for the Aspire-backed localhost auth/session Playwright lane

## Outcome

✅ **Added localhost-auth-playwright job** to `.github/workflows/ci-tests.yml`

### Delivered

- Added dedicated `localhost-auth-playwright` job alongside `storybook-tests` and `core-tests`
- Configured Node 22.17.1 and .NET 10 runtime setup
- Integrated Playwright Chromium browser install with system dependencies
- Added HTTPS dev certificate generation and trust sequence
- Implemented Aspire prerequisite validation (`validate-aspire-prereqs.mjs`)
- Preserved existing Storybook and core test jobs without interference
- Widened workflow path filters to include Aspire-backed auth graph

### Validation

- ✅ Local `npm run test:playwright:localhost-auth` suite passed **8/8**
- ✅ AppHost lifecycle working (startup, two restarts, shutdown)
- ✅ Keycloak auth session persistence verified
- ✅ Bearer token refresh across restarts validated
- ✅ No regressions in existing CI jobs

### Key Decisions

1. **Isolated job design** — Localhost auth lane fails independently without affecting core/storybook paths
2. **Minimal bootstrap** — Used standard GitHub Actions for Node/Playwright/dotnet with explicit cert trust
3. **Real lifecycle** — Delegate to existing `npm run test:playwright:localhost-auth` contract instead of re-encoding YAML logic
4. **Selective gating** — Widened filters to include auth-touched paths: AppHost, TestSite, MockBusinessApp, KeycloakProxy, Shared, keycloak/, and scripts/validate-aspire-prereqs.mjs

### Blockers Addressed

- ✅ HTTPS certificate trust on CI runner (dev-certs https --trust)
- ✅ Aspire prerequisites validation in non-interactive context
- ✅ Port collision prevention (localhost auth uses fixed ports 17214, 15135, 21233, 22194, 44345, 8443, 7245)

## Review Paths

- `.github/workflows/ci-tests.yml` — new job spec
- `src/UmbracoPrism.Client/package.json` — test:playwright:localhost-auth contract
- `src/UmbracoPrism.Client/playwright.localhost-auth.config.ts` — browser config
- `src/UmbracoPrism.Client/tests/support/live-app-host.ts` — lifecycle automation
- `scripts/validate-aspire-prereqs.mjs` — prerequisite validator
- `src/UmbracoPrism.AppHost/Program.cs` — Aspire service config

## Next Steps

- Monitor CI run performance and wall-clock time impact
- Validate certificate trust across matrix runners (ubuntu-latest, macos-latest, windows-latest as needed)
- Consider conditional gating for auth lane on auth-path changes once stable
