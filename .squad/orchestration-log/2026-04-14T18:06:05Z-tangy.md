# Orchestration Log: Tangy — 2026-04-14T18:06:05Z

**Topic:** Add separate GitHub Actions job for the Aspire-backed localhost auth/session Playwright lane

## Outcome

✅ **CI-readiness QA verdict: GREEN**

### Review Scope

Evaluated Blathers' `localhost-auth-playwright` job spec for:
- CI environment prerequisites (Node, .NET, Docker, Playwright)
- HTTPS certificate trust strategy
- Aspire lifecycle automation
- Port collision and resource constraints
- Regression contract alignment with existing tests

### QA Findings

**✅ Passed**
- Certificate trust strategy sound: explicit `dotnet dev-certs https --trust` + HttpClient transport config in test code
- Aspire prerequisites properly validated before test execution
- Browser automation dependencies correctly sequenced (Node → Playwright → Chromium + deps)
- Existing test contracts already executable end-to-end (8/8 passing locally)
- Port usage isolated to localhost auth lane; no collisions with storybook/core jobs

**⚠️ Notes**
- CI wall-clock impact: Expect +3–5 min per PR (AppHost cold start, container pulls, browser install)
- Consider conditional triggers on auth-path changes for production gating
- Monitor ubuntu-latest runner certificate trust; may require CI-specific certificate handling on non-x86 runners

### Security Review

- ✅ HTTPS enforced for real auth flows (Keycloak, TestSite, MockBusinessApp)
- ✅ Dev certificate scope limited to `https://localhost`
- ✅ Bearer token validation and refresh working as expected
- ✅ No hardcoded secrets or credentials in test fixtures

### Performance Expectations

- **AppHost startup:** ~30–60 seconds
- **Keycloak container init:** ~20–30 seconds
- **Playwright browser install:** ~40–60 seconds (cached after first run)
- **Test execution:** ~2–3 minutes
- **Total estimated wall time:** 5–8 minutes per PR

### Recommendation

**APPROVE for merge.** CI job is production-ready. Monitor first few runs for certificate trust issues on ubuntu-latest; if present, add explicit certificate PIN or use test-only certificate bundle strategy.

## Review Paths

- `.github/workflows/ci-tests.yml` — job spec and trigger filters
- `src/UmbracoPrism.Client/tests/support/live-app-host.ts` — lifecycle contract
- `scripts/validate-aspire-prereqs.mjs` — prerequisite validator
- GitHub Actions job logs on first PR merge

## Next Steps

- Merge job spec into main branch
- Trigger first CI run to validate certificate trust on ubuntu-latest
- Document CI wall-time expectations in `.squad/decisions.md` for future auth-path PRs
