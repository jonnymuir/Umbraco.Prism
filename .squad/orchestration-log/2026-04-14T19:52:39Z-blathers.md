# 2026-04-14T19:52:39Z — Blathers (Backend Dev)

## Task
Patch `.github/workflows/ci-tests.yml` to add `workflow_dispatch` trigger and wire `SSL_CERT_DIR` for Linux certificate trust before `dotnet dev-certs https --trust`.

## Mode
Background

## Outcome
✅ **Completed** — Added `workflow_dispatch:` at top level and persisted `SSL_CERT_DIR` on Ubuntu runners with both ASP.NET dev-certs trust directory and system CA paths. YAML validated. Existing job topology preserved.

## Key Decisions
- Kept `.github/workflows/ci-tests.yml` as the single CI workflow (no new file).
- Added `workflow_dispatch:` for manual GitHub UI and `gh` CLI rerun support.
- Set `SSL_CERT_DIR=$HOME/.aspnet/dev-certs/trust:/etc/ssl/certs:/usr/lib/ssl/certs` before trust step on Ubuntu to address exit-4 bootstrap failure.
- Preserved existing `pull_request` and `push` triggers and all job names.

## Implications
- Manual CI runs now include `localhost-auth-playwright` alongside existing lanes.
- Later steps inherit OpenSSL trust context via `$GITHUB_ENV`.
- No breaking changes to workflow topology or job history.
