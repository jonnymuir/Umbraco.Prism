# Session Log — Keycloak ARM64 Startup Fix

**Date:** 2026-04-12  
**Timestamp:** 2026-04-12T07:03:36Z  
**Agent:** Blathers (Backend Dev)  
**Issue:** Keycloak container crashes on Apple Silicon M4 with SIGILL during `java.lang.System.registerNatives()`

## Summary

Fixed Keycloak Aspire startup crash on macOS ARM64 by applying OpenJDK 21 SVE workaround.

- **Root Cause:** OpenJDK 21 `linux-aarch64` SVE bug on Apple Silicon Docker
- **Fix:** Apply `JAVA_OPTS_APPEND=-XX:UseSVE=0` conditionally when AppHost runs on macOS ARM64
- **Files Changed:**
  - `src/UmbracoPrism.AppHost/Program.cs` — Add ARM64 detection and JVM option
  - `ASPIRE_DEV.md` — Document macOS Apple Silicon workaround + fallback guidance
- **Test Results:** All tests pass; Aspire reaches Keycloak startup + realm import successfully

## Decisions Recorded

1. **Keycloak ARM64 startup workaround** — Apply SVE disable only on macOS ARM64
2. **Aspire workload deprecation** — Treat deprecated workload as obsolete; use SDK instead

## Status

✅ **READY FOR MERGE** — All validation passed.
